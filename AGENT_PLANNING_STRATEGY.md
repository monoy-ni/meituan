# 杭州活动规划 Agent Planning 策略

## 1. 文档目的

本文档说明整个 `activity_agent` 的 Planning 策略，即 Agent 如何把一段自然语言需求转成可执行的活动方案，并在后续支持调整、反馈收敛、预约草稿、确认下单和刷新校验。

这份文档关注的是整个 Agent 的决策逻辑，而不是单个 skill 的包装策略。

## 2. Agent 的核心目标

这个 Agent 的目标不是单纯回答“去哪玩”，而是完成一条完整的规划链路：

1. 理解用户意图和场景。
2. 从自然语言中提取最小可用请求。
3. 选择最匹配的主题方向。
4. 从本地供给库中组装完整路线。
5. 输出多个可执行方案，而不是单点推荐。
6. 在用户反馈后继续收敛，而不是重新从零开始。
7. 在预约前后保持安全边界，不直接做不可逆动作。

一句话概括：从“用户一句模糊的话”出发，生成“可执行、可调整、可确认”的杭州本地主题局方案。

## 3. 总体 Planning 链路

整个 Agent 的主链路如下：

```text
用户输入
→ IntentRouter
→ ContextCollector
→ LLM 数据合并（可选）
→ 调整项应用
→ ThemePlanner
→ SupplyMatcher
→ ItineraryComposer
→ ShareCardGenerator
→ 存储与后续反馈/预约链路
```

对应到 `ActivityPlanningAgent.plan()` 的主要流程是：

1. 路由意图与场景
2. 收集上下文
3. 合并 LLM 结构化理解结果
4. 应用基于文本的直接调整
5. 规划主题
6. 组合 itinerary
7. 返回 `PlanningResult`

## 4. Planning 的分层策略

整个 Agent 的规划，不是一次性拍脑袋生成，而是分四层完成：

### 4.1 路由层

负责回答两个问题：

- 用户现在是在“要方案、改方案、提反馈、要预约、做复盘”中的哪一种？
- 当前更像朋友局还是情侣约会？

对应模块：`IntentRouter`

### 4.2 请求建模层

负责把自然语言变成结构化 `UserRequest`，例如：

- 时间
- 地点
- 预算
- 人数
- 心情标签
- 体验标签
- 关系阶段
- 约束条件

对应模块：`ContextCollector`

### 4.3 主题决策层

负责从“很多可能的玩法方向”里，筛出最合适的 3 个主题。

对应模块：`ThemePlanner`

### 4.4 路线组装层

负责把主题里的槽位，映射到实际商户和活动供给，组成完整路线。

对应模块：

- `SupplyMatcher`
- `ItineraryComposer`

## 5. 第一阶段：意图与场景识别策略

Planning 的入口不是直接选路线，而是先判断用户在干什么。

### 5.1 意图识别

当前 Agent 识别以下意图：

- `PLAN`：用户要生成方案
- `ADJUST`：用户要改方案
- `FEEDBACK`：用户提供组局反馈
- `BOOKING`：用户准备预约/下单
- `REVIEW`：用户进行活动复盘

识别方法是轻量关键词路由，例如：

- “复盘、评分、实际人均”触发 `REVIEW`
- “确认预约、下单、支付”触发 `BOOKING`
- “预算太高、不喝酒、晚点到”触发 `FEEDBACK`
- “换、便宜点、室内、改成”触发 `ADJUST`

这里的策略是：

- 先用低成本、稳定的规则完成初筛
- 不把所有理解都压给 LLM
- 保证无 API Key 时系统仍然能跑通

### 5.2 场景识别

场景分为：

- `FRIENDS`
- `COUPLE`

识别依据主要来自用户文本中的关系词：

- “朋友、组局、同事、几个人”倾向 `FRIENDS`
- “约会、对象、暧昧、纪念日、女朋友”倾向 `COUPLE`

如果没有明显线索，默认按 `FRIENDS` 处理。

这么做的原因是：

- 朋友局是更安全的默认场景
- 情侣场景涉及更多关系阶段与安全边界
- 错把朋友当情侣的风险大于反过来

## 6. 第二阶段：用户请求建模策略

意图和场景确定后，Agent 会用 `ContextCollector` 生成一个最小可规划的 `UserRequest`。

### 6.1 最小可规划请求

Agent 不要求用户一次性说全，而是尽快构造一个“够规划”的请求对象。

`UserRequest` 中最关键的字段包括：

- `scene`
- `time_window`
- `location_anchor`
- `budget_per_person`
- `party_size`
- `mood_tags`
- `experience_tags`
- `hard_constraints`
- `journey_duration`
- `weather_sensitive`

情侣场景还会补充：

- `relationship_stage`
- `relationship_goal`

### 6.2 默认值策略

如果用户没有给完整信息，系统会用默认值先跑起来，而不是卡住。

例如：

- 朋友局默认预算约为 220
- 情侣局默认预算约为 360
- 朋友局默认人数约为 4
- 情侣局默认人数为 2
- 未给时间时，朋友局默认今晚 18:30-23:30
- 命中杭州主题标签时，地点锚点会落到 `hangzhou`

这个策略的本质是：

- 先给用户一个可以讨论和修改的方案
- 再通过反馈逐步收敛

### 6.3 标签提取策略

Agent 不直接从原句生成主题，而是先提取两类标签：

- `mood_tags`：描述情绪或氛围，例如“回血、出片、解压、省钱”
- `experience_tags`：描述目的地语义，例如“杭州、老城烟火、西湖、运河、近郊山水、雨天室内”

这样做的好处是：

- 用户表达可以很自由
- 内部决策依赖统一标签体系
- 后续主题评分更稳定

### 6.4 约束提取策略

`hard_constraints` 用来承接“明确不能违背”的要求，例如：

- 不喝酒
- 室内优先
- 更便宜
- 出片
- 安静
- 想要酒店
- 想要礼物
- 允许只参加后半场

在后续供给匹配阶段，这些约束会直接影响候选过滤。

## 7. 第三阶段：LLM 与规则的协同策略

当前 Agent 的核心设计不是“全规则”也不是“全 LLM”，而是规则底座 + 可选 LLM 增强。

### 7.1 规则底座

即使没有 LLM，系统也能完整完成：

- 意图识别
- 场景识别
- 预算/人数/时间解析
- 标签抽取
- 主题选择
- 路线生成

### 7.2 LLM 增强

如果配置了 LLM，系统可以将结构化理解结果通过 `_merge_llm_data()` 合并回 `UserRequest`，增强对以下内容的理解：

- scene
- hard constraints
- mood tags
- experience tags
- budget
- party size
- time window
- location anchor

### 7.3 协同原则

LLM 的角色不是直接替代业务规划，而是：

- 提高自然语言理解质量
- 更好补全结构化字段
- 在复杂表达时减少误解

真正的 Planning 决策仍然由稳定的规则模块完成。

## 8. 第四阶段：主题选择策略

用户请求被结构化后，Agent 不会直接匹配商户，而是先决定“应该走哪种主题方向”。

对应模块：`ThemePlanner`

### 8.1 三类主题池

当前系统主要有三组主题池：

- 朋友局主题
- 情侣约会主题
- 杭州本地一日主题

如果请求中命中杭州本地体验标签，例如：

- 杭州
- 西湖
- 运河
- 老城烟火
- 茶山
- 近郊山水
- 不想查攻略

则优先进入杭州本地主题池。

### 8.2 评分而不是硬匹配

主题不是一对一命中，而是打分排序后取 Top 3。

评分参考包括：

- 触发标签命中数
- 体验标签命中数
- 是否匹配时长
- 是否命中下雨/室内偏好
- 是否命中低预算偏好
- 是否命中拍照偏好
- 是否匹配关系阶段和关系目标

### 8.3 场景专属策略

#### 朋友局

朋友局更多由 `mood_tags` 驱动，例如：

- 回血
- 出片
- 发疯
- 解压
- 省钱

#### 情侣局

情侣局更多由 `relationship_stage` 和 `relationship_goal` 驱动，例如：

- 暧昧/追求中
- 稳定情侣
- 纪念日
- 想修复关系
- 想制造惊喜

#### 杭州局

杭州局更多由 `experience_tags` 和地理语义驱动，例如：

- 老城烟火
- 运河
- 西湖
- 茶山
- 雨天室内

## 9. 第五阶段：供给匹配策略

主题选出来后，Agent 要为每个主题槽位挑具体供给。

对应模块：`SupplyMatcher`

### 9.1 先过滤，再打分

每个槽位的匹配分两步：

1. 候选过滤
2. 候选评分

### 9.2 候选过滤规则

供给需要满足以下条件：

- 当前可用
- 未被本方案其他槽位使用
- 类型匹配槽位类型
- 场景匹配当前用户场景
- 通过硬约束过滤

硬约束过滤包括：

- 不喝酒时过滤微醺/酒相关供给
- 室内优先时过滤明显室外项目
- 雨天敏感时优先有雨天适配的供给
- 安静偏好时过滤太闹腾的供给
- 暧昧期默认过滤酒店

### 9.3 评分策略

通过过滤后，候选会按综合分排序，维度包括：

- 槽位标签匹配度
- 当前 mood/experience 标签匹配度
- 区域命中
- 天气匹配度
- 可打卡性
- 价格适配度
- 距离
- 是否可预约
- 商户画像评分
- 是否命中外部搜索关键词
- 是否被用户显式偏好

这里体现的是典型的“多目标打分”策略，而不是只看价格或只看标签。

## 10. 第六阶段：路线组装策略

有了主题和供给后，系统会用 `ItineraryComposer` 组装成完整方案。

### 10.1 基本组装逻辑

每个主题包含若干 `ThemeSlot`，例如：

- Activity
- Dining
- Relax
- Checkin
- Nightlife
- Hotel

系统会按槽位顺序生成 timeline，并自动安排：

- 开始时间
- 结束时间
- 每段之间约 20 分钟转场
- 总成本
- 总距离
- 预约准备度

### 10.2 情侣场景附加逻辑

情侣场景下，如果用户有：

- `gift_wanted`
- 纪念日
- 想制造惊喜

系统会尝试追加 `GIFT` 槽位，形成更完整的表达闭环。

### 10.3 风险提示策略

每个 `PlanOption` 都会自动生成 `risk_notes`，例如：

- 当前预估超预算
- 暧昧期不安排酒店
- 已按不喝酒过滤
- 酒店或夜宿必须二次确认
- 天气/库存/路线为 seed 估算

这样用户拿到的不只是“推荐”，还有风险可见性。

### 10.4 可执行动作策略

方案中还会生成 `actions`，让用户知道下一步怎么推进，例如：

- 就按这个走
- 更近一点
- 便宜点
- 少走路
- 改室内
- 一键预约

这本质上是在 Planning 输出中预埋后续交互入口。

## 11. 第七阶段：方案输出策略

一次 Planning 不只返回一个方案，而是通常返回 3 个方向。

### 11.1 为什么返回多个方案

因为用户真实需求往往不是一次性说准的。

多方案可以帮助用户：

- 比较主题方向
- 看清取舍
- 在低成本调整中快速收敛

### 11.2 每个方案包含什么

每个 `PlanOption` 至少包含：

- 主题名
- 情绪 hook
- 时间线
- 预估人均
- 总距离
- 预约准备度
- 风险提示
- 可替换槽位
- 加项建议
- 邀约文案或约会提示
- 路线说明
- 置信度

### 11.3 分享卡策略

规划结果会进一步生成 share card，用于：

- 发群
- 发给朋友
- 用于方案比较

这说明 Agent 的输出不是只服务“自己看”，还服务“传播和协同决策”。

## 12. 第八阶段：反馈收敛策略

用户第一次看到方案后，系统默认认为需求还会继续收敛。

对应模块：`FeedbackResolver`

### 12.1 反馈来源

反馈主要包括：

- 预算反馈
- 时间反馈
- 不喝酒/室内等边界反馈
- 只能参加部分行程
- 晚到

### 12.2 收敛逻辑

反馈不是只改一句话，而是重新生成一个更紧的 `UserRequest`：

- 预算向最保守值收敛
- 人数按有效参与者收敛
- mood tags 叠加偏好标签
- hard constraints 叠加边界条件

然后重新调用：

```text
ThemePlanner
→ ItineraryComposer
```

### 12.3 保留方向，不推翻一切

如果用户已经偏好某个主题方向，系统会优先保留该主题，避免每次反馈都彻底换风格。

这个策略很重要，因为真实用户通常想“改进这个方案”，而不是“重新来一个完全不同的”。

## 13. 第九阶段：多轮引导式 Planning 策略

除了直接 `chat()` 规划，系统还支持 `chat_with_guidance()`。

### 13.1 目的

用于处理这类情况：

- 用户需求非常模糊
- 用户不擅长一次性给完整条件
- 需要通过自然对话逐步收集信息

### 13.2 策略

多轮引导并不是一直追问，而是由 `DialogueManager` 做状态机控制：

- 先识别人群
- 再逐步收集预算、时间、氛围等核心信息
- 一旦达到可规划阈值，就自动触发 `plan()`

### 13.3 好处

相比一口气问很多问题，这种方式：

- 更像真人对话
- 压力更低
- 容易收集真实需求
- 更适合前端聊天场景

## 14. 第十阶段：预约前后的 Planning 边界

规划不是在出方案时结束，Agent 还负责把方案往“可执行”推进，但不会跨越安全边界。

对应模块：`BookingOrchestrator`

### 14.1 创建预约草稿

在 `create_booking_draft()` 阶段，系统会：

- 检查每个时间线项目的可用性
- 创建 mock hold
- 根据场景生成支付模式
- 朋友局可生成 AA 草稿

但这个阶段不会直接支付或确认下单。

### 14.2 确认预约

只有用户显式确认后，才会执行 `confirm_booking()`，生成 mock 确认订单。

这体现了一个关键策略：

- Planning 可以半自动推进
- 不可逆动作必须显式确认

### 14.3 规划与预约的关系

Agent 的定位不是旅游推荐器，也不是纯交易系统，而是：

- 先规划
- 再收敛
- 再校验
- 最后在明确授权下确认执行

## 15. 第十一阶段：刷新与数据置信度策略

由于当前很多数据仍是 seed/mock 模式，系统在规划时必须显式暴露数据置信度。

### 15.1 置信度层级

当前主要有：

- `seed`
- `cache`
- `realtime/live/official`

### 15.2 refresh 策略

`refresh_itinerary()` 会对已有方案做一次刷新检查，补充：

- 路线信息
- 天气信息
- 商户可用性

即使没有真实 provider，也会明确返回：

- 当前是 seed 估算
- 建议出发前确认

这种策略的核心不是“装作真实”，而是“透明地告诉用户哪些是估算值”。

## 16. Planning 的优先级原则

整个 Agent 的 Planning 决策遵循如下优先级：

1. 安全边界
2. 场景匹配
3. 用户硬约束
4. 主题一致性
5. 可执行性
6. 预算适配
7. 情绪价值
8. 出片和传播感

也就是说：

- 宁可少一点花样，也不能突破关系边界
- 宁可保守一点，也不能违背“不喝酒、室内、便宜”等硬要求
- 宁可路线简单一点，也要保证能实际走通

## 17. Agent Planning 的质量标准

一套合格的 Agent 规划结果，应同时满足：

- 能解释为什么是这个主题
- 能落到具体商户和路线
- 预算、时间、距离大致合理
- 有清晰的风险提示
- 用户可继续调整
- 可以进入预约草稿阶段

如果只是“推荐几个店”，这不是完整的 Agent Planning。

如果只是“给一条漂亮路线”，但不能反馈收敛、不能预约、不能校验，也不是完整的 Agent Planning。

## 18. 一句话总结

整个 `activity_agent` 的 Planning 策略，本质上是一个“分层收敛系统”：

先用规则和可选 LLM 把自然语言收敛成结构化请求，再用主题规划和供给匹配把请求收敛成可执行路线，最后通过反馈、刷新和预约链路把路线收敛成真正可落地的行动方案。
