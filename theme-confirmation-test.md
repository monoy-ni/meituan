# 主题确认节点测试文档

## 1. 目标

验证系统在正式生成方案前，能够先通过对话与用户交流，完成主题方向确认相关流程，包括：

- 识别用户场景：朋友局 / 情侣约会
- 收集主题相关关键信息：氛围、预算、时间、关系阶段
- 在信息足够时进入方案生成
- 在信息不足时继续追问，而不是直接出方案

本测试文档聚焦“先交流确认主题”这一节点，不覆盖预约、支付、复盘等后续流程。

## 2. 节点定义

当前代码中，“确认主题”不是一个单独枚举状态，而是由以下连续流程共同构成：

1. 识别场景
2. 收集主题相关信息
3. 判断是否达到可生成方案的条件
4. 进入 `READY_TO_PLAN`

对应核心代码：

- `activity_agent/agent.py` 中 `chat_with_guidance()`
- `activity_agent/modules/dialogue_manager.py` 中 `process_input()`
- `activity_agent/modules/dialogue_manager.py` 中 `_generate_response()`
- `activity_agent/modules/dialogue_manager.py` 中 `_next_step()`
- `backend/main.py` 中 `/api/chat/guided`
- `frontend/src/App.js` 中 `sendGuided()` 与 `replySuggestions`

## 3. 测试范围

本轮测试覆盖：

- 首轮输入时的场景识别
- 场景不明确时的补充确认
- 朋友局主题方向确认
- 约会主题方向确认
- 关键字段缺失时的追问逻辑
- 关键字段收集完成后是否触发方案生成
- 前端对 `conversation.next_step` 的展示配合

本轮测试不覆盖：

- 方案内容质量本身
- 实时 POI 搜索结果质量
- 预约草稿创建
- 订单确认

## 4. 相关状态

与本节点直接相关的状态：

- `INIT`
- `IDENTIFYING_SCENE`
- `COLLECTING_FRIENDS_CONTEXT`
- `COLLECTING_COUPLE_CONTEXT`
- `READY_TO_PLAN`
- `PRESENTING_OPTIONS`

与前端联动的 `next_step`：

- `choose_scene`
- `ask_mood`
- `ask_budget`
- `ask_time`
- `ask_couple_feeling`
- `generate_options`

## 5. 入口与接口

### 5.1 后端接口

- `POST /api/sessions`
- `POST /api/chat/guided`
- `GET /api/conversation-state/{session_id}`

### 5.2 前端入口

- 首页聊天输入框
- 发送后由 `sendGuidedChatMessage()` 调用 `/api/chat/guided`

## 6. 通过标准

满足以下条件视为通过：

- 用户首次输入后，系统能正确判断是否需要先确认场景
- 系统追问内容与当前缺失字段一致
- 收集完关键信息前，不应直接返回方案列表
- 收集完关键信息后，应进入生成方案阶段
- 返回的 `conversation.next_step` 与当前对话状态一致
- 当前端拿到 `options` 后，页面应切换到方案展示阶段

## 7. 测试前准备

### 7.1 服务准备

- 前端启动：`http://localhost:3000`
- 后端启动：`http://localhost:8000`
- API 文档：`http://localhost:8000/docs`

### 7.2 环境说明

- 如果配置了真实 LLM，优先走 LLM 理解
- 如果未配置 LLM，自动降级到规则理解
- 本测试两种模式都应能完成主题确认

## 8. 测试数据建议

建议使用以下输入类型：

- 明确场景：`今晚和朋友聚聚`
- 模糊场景：`今晚想出去玩`
- 明确氛围：`想轻松一点`、`想拍照出片`
- 明确预算：`人均 200`
- 明确时间：`周六下午`
- 约会线索：`纪念日`、`怕尴尬`、`想自然一点`

## 9. 核心测试用例

### TC-01 首轮即可识别朋友局

**目的**

验证用户首句已明确表达朋友场景时，系统直接进入朋友局信息收集。

**步骤**

1. 创建会话
2. 发送：`今晚想和朋友聚聚`

**预期结果**

- `scene` 被识别为 `friends`
- 对话状态进入 `COLLECTING_FRIENDS_CONTEXT`
- 回复应继续追问氛围、预算或时间，而不是再次问“你是朋友还是约会”
- `next_step` 应为 `ask_mood`、`ask_budget` 或 `ask_time` 中之一

### TC-02 首轮无法识别场景时先确认

**目的**

验证系统在信息不足时先确认场景，而不是直接猜测。

**步骤**

1. 创建会话
2. 发送：`今晚想出去玩`

**预期结果**

- 状态进入 `IDENTIFYING_SCENE`
- 回复应类似“是想和朋友聚聚，还是想安排约会”
- `next_step = choose_scene`
- 不返回方案列表

### TC-03 模糊场景后二次输入确认朋友局

**目的**

验证从场景确认过渡到朋友局主题确认。

**步骤**

1. 先执行 TC-02
2. 第二轮发送：`和朋友`

**预期结果**

- 状态切换为 `COLLECTING_FRIENDS_CONTEXT`
- 回复应进入朋友局收集语境
- 开始围绕“怎么玩、预算、时间”继续追问

### TC-04 朋友局主题确认完整闭环

**目的**

验证朋友局在收集完成后进入方案生成。

**步骤**

1. 发送：`今晚和朋友聚聚`
2. 发送：`想放松回血`
3. 发送：`人均 200`
4. 发送：`今晚`

**预期结果**

- 第一轮后进入 `COLLECTING_FRIENDS_CONTEXT`
- 第二轮后已记录 `mood`
- 第三轮后已记录 `budget`
- 第四轮后已记录 `time`
- 随后进入 `READY_TO_PLAN`
- 返回方案列表 `options`
- 前端切换到方案展示页

### TC-05 首轮即可识别约会场景

**目的**

验证用户首句已明确表达约会场景时，系统直接进入约会信息收集。

**步骤**

1. 创建会话
2. 发送：`周末想和女朋友约会`

**预期结果**

- `scene = couple`
- 状态进入 `COLLECTING_COUPLE_CONTEXT`
- 回复应围绕约会感觉、预算、时间继续追问
- `next_step` 应优先为 `ask_couple_feeling` 或相关字段

### TC-06 纪念日线索触发约会主题确认

**目的**

验证隐式约会线索可以被识别。

**步骤**

1. 发送：`下周是纪念日，想安排一下`

**预期结果**

- 应识别为 `couple`
- 若有关系阶段字段，应优先接近“纪念日”
- 回复语气应偏仪式感场景
- 后续继续追问预算和时间

### TC-07 暧昧场景触发轻松主题确认

**目的**

验证“怕尴尬”等线索会引导到合适的约会主题方向。

**步骤**

1. 发送：`想约她出来，但是怕尴尬`

**预期结果**

- 应识别为 `couple`
- 关系阶段倾向“暧昧/追求中”
- 回复应体现“轻松不尴尬”方向
- 不应直接给出过于强烈的纪念日式方案

### TC-08 一句话给全信息时直接生成

**目的**

验证系统在信息已经充分时，不做多余追问。

**步骤**

1. 发送：`周六下午和朋友聚聚，想轻松一点，人均 150`

**预期结果**

- 识别 `scene = friends`
- 同时收集到 `mood`、`budget`、`time`
- 可直接进入 `READY_TO_PLAN`
- 返回方案列表

### TC-09 缺预算时必须追问

**目的**

验证缺失关键字段时，系统不会跳过。

**步骤**

1. 发送：`周末想和朋友出去玩，最好能拍照`

**预期结果**

- 已识别场景和氛围
- 因缺少预算，系统应追问预算
- `next_step = ask_budget`
- 不应直接生成方案

### TC-10 缺时间时必须追问

**目的**

验证时间字段缺失时系统会继续确认。

**步骤**

1. 发送：`想安排一个约会，人均 300，想浪漫一点`

**预期结果**

- 已识别 `couple`
- 已收集主题感觉与预算
- 因缺少时间，应追问时间
- `next_step = ask_time`

## 10. 接口级测试示例

### 10.1 创建会话

```http
POST /api/sessions
```

### 10.2 引导式对话

```http
POST /api/chat/guided
Content-Type: application/json

{
  "session_id": "会话ID",
  "message": "今晚想和朋友聚聚",
  "scene_hint": null
}
```

### 10.3 关键返回字段校验

重点检查：

- `message`
- `scene`
- `options`
- `conversation.state`
- `conversation.next_step`
- `conversation.should_show_options`

## 11. 前端联调检查点

前端需要确认：

- 首轮发送后，聊天区正常展示 assistant 追问
- 当 `conversation.next_step` 变化时，建议回复内容同步变化
- 未拿到 `options` 时，仍停留在聊天流程
- 拿到 `options` 后进入主题方案展示

## 12. 异常与边界测试

### 12.1 用户反复不给关键信息

输入示例：

- `随便`
- `都行`
- `你看着办`

预期：

- 系统继续围绕缺失字段追问
- 不应立刻输出无依据方案

### 12.2 中途切换主题方向

步骤：

1. 先输入：`和朋友聚聚`
2. 再输入：`其实更想安静一点，别太累`

预期：

- 后续主题方向应向“轻松/低体力/安静”调整
- 仍处于主题确认流程，不应异常跳状态

### 12.3 中途更换场景

步骤：

1. 输入：`今晚想出去玩`
2. 系统追问
3. 用户回复：`其实是约会`

预期：

- 场景应切换到 `couple`
- 后续按约会逻辑继续追问

## 13. 验收结论模板

可按以下模板记录结果：

| 用例编号 | 执行结果 | 备注 |
| --- | --- | --- |
| TC-01 | 通过/失败 |  |
| TC-02 | 通过/失败 |  |
| TC-03 | 通过/失败 |  |
| TC-04 | 通过/失败 |  |
| TC-05 | 通过/失败 |  |
| TC-06 | 通过/失败 |  |
| TC-07 | 通过/失败 |  |
| TC-08 | 通过/失败 |  |
| TC-09 | 通过/失败 |  |
| TC-10 | 通过/失败 |  |

## 14. 建议补充

如果后续要把“确认主题”做成更明确的产品节点，建议补充：

- 单独的“主题确认”状态枚举
- 明确的“主题候选确认”接口返回结构
- 前端可视化的主题确认卡片
- 针对主题确认节点的自动化接口测试
