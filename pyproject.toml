[project]
name = "activity-planning-agent"
version = "1.4.0"
description = "Python MVP for Hangzhou local day-itinerary planning, seed providers, booking drafts, and review memory."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
